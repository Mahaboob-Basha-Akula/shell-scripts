This is class 1 for learning shell script

In this frist class we have discussed about what is shell, what are advantages of it , and why we are using.

#!/bin/bash ---- it is called shebang

It should be in the first line and it is interpreter means it checks the scripts.There are more scripts previously and finally used only one i.e bash

shell file extension is .sh 
y we are using is for readabilty, when we see the .sh we can easliy identity this is shell script file
